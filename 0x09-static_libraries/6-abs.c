#include "main.h"

/**
 * _abs - check main
 * @i: an integer input
 *
 * description: returns absolute value of an integer
 * Return: absolute value of integer
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
